[project]
name = "asios-execution-set"
version = "1.0.0"
description = "Deterministic execution set with registry-bound governance"
requires-python = ">=3.11"
dependencies = []
[project.scripts]
asiosctl = "asiosctl:main"
[tool.setuptools]
packages = ["decision_decomposition"]
